function rangoAleatorio(){
      const MIN = 0;
      const MAX = 10;

      let a = 7;
      let b = 12;
      let aleatorio = Math.floor((Math.random() * (MAX - MIN + 1)) + MIN) 
      console.log(aleatorio);

      

}